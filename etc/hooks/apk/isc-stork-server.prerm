#!/bin/sh

set -eu

if command -v rc-service > /dev/null; then
  # Stop if started.
  rc-service --ifstarted isc-stork-server stop
fi

groups stork-server | xargs -n 1 | grep -v stork-server | xargs -r -n 1 delgroup stork-server
